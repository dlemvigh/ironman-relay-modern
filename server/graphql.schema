type Discipline {
  _id: ID
  id: String
  name: String
  score: Float
  unit: String
  order: Int
}

type Query {
  disciplines: [Discipline]
  user(id: String!): User
  users: [User]
}

type User {
  _id: ID
  id: String
  name: String
  active: Boolean
}
